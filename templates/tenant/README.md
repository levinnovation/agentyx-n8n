# {{tenant_name}}

This is a scaffolded tenant. Replace this README with real content.

## Domains

- `example-domain/` — replace with real domain

## Quick Start

```bash
make scaffold-domain TENANT={{tenant_id}} DOMAIN=your-domain
```
