id: "{{asset_id}}"
name: "{{asset_name}}"
type: deployment
tenant: "{{tenant_id}}"
domain: "{{domain_id}}"
capability: "{{capability_id}}"
status: scaffolded
description: "Multi-platform deployment configurations."
owner: "replace@example.com"
observability:
  traces: none
  metrics: none
