id: "{{asset_id}}"
name: "{{asset_name}}"
type: channel-adapter
tenant: "{{tenant_id}}"
domain: "{{domain_id}}"
capability: "{{capability_id}}"
status: scaffolded
description: "WhatsApp channel adapter via Kapso."
owner: "replace@example.com"
observability:
  traces: langfuse
  metrics: none
