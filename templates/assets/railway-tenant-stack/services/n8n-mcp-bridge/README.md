# n8n MCP Bridge (template service)

Bridge service exposing selected n8n workflows as MCP tools.

## Variables

- `MCP_AUTH_TOKEN`
- `N8N_BRIDGE_SECRET`
- `N8N_TOOLS_REGISTRY_JSON`

## Runtime endpoint

- `POST /mcp`
- `GET /healthz`
