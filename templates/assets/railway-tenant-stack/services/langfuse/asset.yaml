id: langfuse
name: Langfuse
type: deployment
tenant: "{{tenant_id}}"
domain: "{{domain_id}}"
capability: "{{capability_id}}"
status: active
description: LLM observability and tracing platform.
observability:
  traces: langfuse
  metrics: none
deployment:
  platform: railway
  region: us-east-1
