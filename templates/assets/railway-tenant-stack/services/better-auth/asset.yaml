id: better-auth
name: Better Auth
type: auth
tenant: "{{tenant_id}}"
domain: "{{domain_id}}"
capability: "{{capability_id}}"
status: active
description: |
  Per-tenant auth federation service exposing Better Auth + OIDC provider +
  Google Workspace social login + forward-auth endpoint for Caddy.
observability:
  traces: none
  metrics: none
deployment:
  platform: railway
  region: us-east-1
