{
  "name": "better-auth-service",
  "version": "1.1.0",
  "description": "Per-tenant Better Auth federation service (Fastify)",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "generate": "npx @better-auth/cli generate"
  },
  "dependencies": {
    "@better-auth/oauth-provider": "^1.2.0",
    "better-auth": "^1.2.0",
    "dotenv": "^16.4.0",
    "fastify": "^5.0.0",
    "nodemailer": "^8.0.7",
    "pg": "^8.13.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/nodemailer": "^8.0.0",
    "@types/pg": "^8.11.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.6.0"
  }
}
