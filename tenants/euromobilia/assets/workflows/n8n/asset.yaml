id: n8n
name: n8n workflow exports (kitchen-quotation)
type: n8n-workflow
tenant: euromobilia
domain: kitchen-commerce
capability: kitchen-quotation
status: validated
version: "1.1.0"
description: |
  Versioned n8n workflow JSON for kitchen-quotation. Runtime: self-hosted
  n8n CE on Hostinger VPS. 3 of 4 workflows active; human-handoff pending
  Slack webhook configuration.
owner: "ai-team@euromobilia.com"
observability:
  traces: none
  metrics: none
deployment:
  platform: self-hosted
  region: hostinger

---

# n8n Workflows Status

| Workflow | File | n8n Status | Webhook Path |
|----------|------|------------|--------------|
| Kapso Inbound Quotation | `kapso-inbound-quotation.json` | Active | `POST /webhook/kapso-inbound` |
| Quote Document Generation | `quote-document-generation.json` | Active | `POST /webhook/quote-document-generation` |
| KB Ingest | `kb-ingest.json` | Active | (schedule) |
| Human Handoff | `human-handoff.json` | Inactive | `POST /webhook/euromobilia-handoff` |

## Required Environment Variables

- `KAPSO_API_KEY`
- `KAPSO_BASE_URL`
- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `SLACK_WEBHOOK_URL` (for human-handoff)
- `AGENT_BASE_URL` (point to deployed quotation-assistant)
