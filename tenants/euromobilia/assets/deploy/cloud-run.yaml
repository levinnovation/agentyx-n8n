apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: quotation-assistant
spec:
  template:
    spec:
      containers:
        - image: gcr.io/PROJECT/quotation-assistant
          ports:
            - containerPort: 8000
