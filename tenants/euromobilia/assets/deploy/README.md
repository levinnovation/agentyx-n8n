# Deployment Assets

Multi-target deployment manifests for the kitchen-quotation capability.

## Self-hosted n8n (Hostinger)

- **`n8n-hostinger/`** — Docker Compose stack (n8n CE + Postgres + Caddy) for VPS `187.127.252.161`. See [n8n-hostinger/README.md](n8n-hostinger/README.md).

Other files in this directory (Render, Railway, Fly, Cloud Run) are legacy/scaffold manifests and are not validated as separate asset subfolders until moved under a named asset directory.
