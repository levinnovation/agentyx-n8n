# Customer Service Action Core

Specialized action core for customer-service intents (CRM/calendar/email actions).

Initial implementation delegates to `customer-service-core` to preserve existing behavior while multi-agent routing is introduced.
