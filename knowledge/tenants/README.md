# Tenant knowledge

Tenant-specific durable notes: decisions, migrations, glossaries—**without** replacing `tenants/{tenant}/` specs.

Each tenant gets a subfolder under `knowledge/tenants/<tenant>/`.
