# Open Codex session bootstrap (minimal)

```text
AGENTS.md + repo-context.md first.
Model: tenant→domain→capability→assets. Forbidden: root agents/workflows/apps.
After edits: make validate. Knowledge: ADR + prompt log + change record if major.
```

Same task recipes as `opencode-session-bootstrap.md` (keep DRY; both tools use compact copy-paste).
