# Open Codex context (compact)

Copy-paste bootstrap: `knowledge/context-packs/open-codex-session-bootstrap.md`

## Recipes

Same as `.opencode/context.md` (tenant, capability, asset, common util, major change). Keep diffs minimal; update `knowledge/` when architecture changes.

## Session discipline

Read packs → state planned paths → edit → `make validate`.
